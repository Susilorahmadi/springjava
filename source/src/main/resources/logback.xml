<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level)[%X{app}][%X{corrId}] %-18logger{0} * %msg%n</pattern>
        </encoder>
    </appender>
    <logger name="org.springframework.retry" level="DEBUG" />
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
